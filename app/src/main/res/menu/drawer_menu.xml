<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/BackToChatButton"
        android:icon="@drawable/chat_icon"
        android:orderInCategory="100"
        android:title="@string/BackToChatButton"
        app:showAsAction="always" />  <!-- This uses showAsAction=always, so look at icon= parameter -->

    <item
        android:id="@+id/BackToProfileButton"
        android:icon="@drawable/profile_icon"
        android:orderInCategory="100"
        android:title="@string/BackToProfileButton"
        app:showAsAction="always" />  <!-- This uses showAsAction=always, so look at icon= parameter -->

</menu>